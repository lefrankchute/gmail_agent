{
  "name": "gmail-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint packages/*/src/**/*.ts",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
